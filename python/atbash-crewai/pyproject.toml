[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "atbash-crewai"
version = "0.1.0"
description = "Atbash Guard for CrewAI tools"
dependencies = [
    "crewai",
    "requests"
]